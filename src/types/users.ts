/**
 * User type definitions for managers and clients
 *
 * Re-exports from the access plugin for backward compatibility.
 * These types are now defined in src/lib/access/types.ts
 */

export type { TypedClient, TypedManager } from '@/lib/access'
